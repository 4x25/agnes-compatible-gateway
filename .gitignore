# dotenv environment variable files. Keep every environment-specific spelling
# out of commits; the empty public template is the sole exception.
.env*
!.env.example

# Fresh build directory
_fresh/
# npm + other dependencies
node_modules/
vendor/

# Test and browser artifacts
.coverage/
coverage/
playwright-report/
test-results/
