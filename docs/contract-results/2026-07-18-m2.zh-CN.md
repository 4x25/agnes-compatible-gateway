# Agnes 契约证据 — 2026-07-18

[English](2026-07-18-m2.md)

状态：**部分验收；M2 保持进行中**。

本文只记录有长度限制的状态和结构观察结果，不包含凭据、提示词、响应值、媒体 URL、
Base64 数据或上游错误正文。

## 运行信息

| 项目       | 值                                           |
| ---------- | -------------------------------------------- |
| 网关 SHA   | `5012b62033c5269932e087ff1ceab010cc255fba`   |
| Deno       | `2.9.3`                                      |
| 上游主机   | `apihub.agnes-ai.com`                        |
| Chat scope | `chat`、`chat-sse`、`chat-tools`、`errors`   |
| 图片 scope | `image`、`image-base64`、`image-edit`        |
| 重试策略   | 所有生成请求均未重试                         |
| 凭据       | 一次性调用方 Key；只加载到显式启用的测试进程 |

## 脱敏结果

| 探测项            | 状态   | 结果                                                                             | 请求 ID     |
| ----------------- | ------ | -------------------------------------------------------------------------------- | ----------- |
| Chat JSON         | `200`  | 通过；存在 OpenAI 风格的 ID、模型、choices、usage 与 metadata 字段类型           | `a1d0…-IAD` |
| Chat SSE          | `200`  | 通过；JSON data 事件包含预期分块字段，流以 `[DONE]` 结束                         | `a1d0…-IAD` |
| 强制工具调用      | `200`  | 通过；assistant 消息包含标准函数工具调用                                         | `a1d0…-IAD` |
| 工具结果往返      | `200`  | 通过；Agnes 接受标准 assistant 工具调用及后续 `tool` 结果消息序列                | `a1d0…-IAD` |
| 非法请求          | `400`  | 通过；正文结构为 `{ error: { code, message, type } }`                            | `a1d0…-IAD` |
| 非法凭据          | `401`  | 使用探测脚本的固定假 Token 通过；正文结构为 `{ error: { code, message, type } }` | `a1d0…-IAD` |
| 图片 URL 输出     | `503`  | 等待响应后被上游可用性阻塞；响应正文按设计未保留                                 | 未输出      |
| 图片 Base64 输出  | 未运行 | 探测按顺序执行，前一个可能计费的生成请求失败后跳过                               | 不适用      |
| Data URI 图片编辑 | 未运行 | 探测按顺序执行，前一个可能计费的生成请求失败后跳过                               | 不适用      |

## 对里程碑的影响

成功的对话与错误探测确认了网关现有对话实现所依赖的上游信封。工具结果观察不会
扩大网关公开支持的消息角色子集；该限制仍是已记录的兼容差异。

本次上游 HTTP `503` 是可用性结果，不能证明图片请求或响应契约不兼容。项目遵循
不重试策略，因此没有重复该请求，也没有启动后续图片探测。只有 URL 输出、Base64
输出和 Data URI 图片编辑在后续受控运行中全部成功后，M2 才能标记完成。
