# Deno Deploy 与发布证据 — 2026-07-19

[English](2026-07-19-m5.md)

状态：**验收通过；M5 已完成**。

本文只记录公开部署元数据、有界的状态/结构观察和脱敏请求 ID，不包含凭据、提示词、
任务或媒体 ID、响应值、媒体 URL、Base64 数据或媒体字节。

## 运行信息

| 项目            | 值                                                                        |
| --------------- | ------------------------------------------------------------------------- |
| 探测源码 SHA    | `90e919af796230c9b0aa518d6737af2af41d2b1d`                                |
| Deno            | `2.9.3`                                                                   |
| Chromium        | `146.0.7680.177`                                                          |
| 部署地址        | `https://agnes-compatible-gateway.zo.deno.net`                            |
| 平台观察        | Deno Deploy `deployd`、HTTP/2，以及 Deno 缓存/追踪响应头                  |
| 部署 Scope      | `health`、`chat-sse`、`image-upload`、`video`                             |
| 凭据            | 一次性、调用方自有 Agnes Key；只加载到显式启用的本地探测进程              |
| 生成重试策略    | 不重试                                                                    |
| 正式源码/Tag    | `cdee3417cd344cfc3b099b0d3245242e12e728e1` / `v0.1.0`                     |
| 正式 GHCR Index | `sha256:008a93110cd362beffa760859e7ed25fbe85a0be2ead76fc05a001871fbbc64f` |
| GitHub Release  | `https://github.com/4x25/agnes-compatible-gateway/releases/tag/v0.1.0`    |

## 脱敏结果

| 探测项             | 状态  | 结果                                                                                     | 请求 ID     |
| ------------------ | ----- | ---------------------------------------------------------------------------------------- | ----------- |
| 健康检查           | `200` | JSON 状态结构、`Cache-Control: no-store` 与 Deno Deploy 平台响应头                       | 未提供      |
| CORS 预检          | `204` | 通配 Origin、预期方法/请求头、公开网关响应头与请求 ID                                    | `aac3…bdb3` |
| 外部 Chromium      | 通过  | 双语、六档响应式宽度、键盘/焦点、reduced-motion 与凭据不持久化                           | 未提供      |
| Chat SSE           | `200` | `text/event-stream`、OpenAI 分块字段类型、至少一个 JSON 事件，并以 `[DONE]` 结束         | `3496…4ddb` |
| multipart 图片编辑 | `200` | 合成 PNG 通过公开编辑接口上传，并返回 Base64 结果字段                                    | `a318…4c0f` |
| 视频创建           | `200` | 公开/video/task 身份字段与异步元数据类型                                                 | `e077…83ea` |
| 视频终态查询       | `200` | 通过公开无状态 video-ID 路径到达成功终态，并返回 URL 字段类型                            | `0836…d4e6` |
| 视频内容 Range     | `206` | 公开内容接口返回第 0 字节与 `Content-Range`；媒体字节仅用于有界断言                      | `2382…d42d` |
| GHCR 正式工作流    | 通过  | 发布 amd64/arm64、SPDX SBOM、SLSA provenance、OCI 标签、非 root 运行时及强化容器健康检查 | 公开运行    |
| GitHub Release     | 通过  | 公开 `v0.1.0` Release 已存在，且不是 Draft 或 Prerelease                                 | 公开 URL    |

## 浏览器与 API 覆盖

外部 Chromium 运行有意不在测试台提交生成请求，因为远端托管网关无法安全改指向本地
假 Agnes；真实部署上的 UI、无障碍、响应式和凭据内存检查全部通过。随后，具有独立
安全开关的部署探测使用一次性调用方 Key，通过同一 Origin 直接执行了全部平台敏感的
公开工作流。

图片编辑跨越 Deno Deploy 请求生命周期完成，确认平台可以承载 multipart 上传与较长
上游响应。视频创建、重复无状态查询、终态媒体解析和公开字节范围代理也全部完成。
每个生成请求只执行一次；只有幂等视频读取会重复。

## 对发布的影响

正式[发布工作流](https://github.com/4x25/agnes-compatible-gateway/actions/runs/29671245362)
发布了可匿名拉取、包含 `linux/amd64` 与 `linux/arm64` 的 OCI Index。每种架构均有
独立 SPDX 与 SLSA 证明 Manifest，以无特权 `deno` 用户运行，并将已验收源码
`cdee341` 同时写入 OCI revision 与 `DENO_DEPLOYMENT_ID`。工作流检查远端 Index
后，以只读文件系统、移除全部 capabilities 的方式成功启动已发布 digest。

独立匿名 Registry 检查确认 `0.1.0`、`0.1`、`0`、`latest` 与 `sha-cdee341`
均指向同一个正式 Index digest。公开 GitHub Release 已发布，且不是 Draft 或
Prerelease。结合真实 Deno Deploy 闭环，这些结果满足 M5 的全部部署、容器、文档与
公开发布门槛；M5 正式完成。
