# Deno Deploy and release evidence — 2026-07-19

[简体中文](2026-07-19-m5.zh-CN.md)

Status: **accepted; M5 complete**.

This record contains only public deployment metadata, bounded status/shape
observations, and redacted request IDs. It omits credentials, prompts, task and
media IDs, response values, media URLs, Base64 data, and media bytes.

## Run metadata

| Item                    | Value                                                                              |
| ----------------------- | ---------------------------------------------------------------------------------- |
| Probe source SHA        | `90e919af796230c9b0aa518d6737af2af41d2b1d`                                         |
| Deno                    | `2.9.3`                                                                            |
| Chromium                | `146.0.7680.177`                                                                   |
| Deployment              | `https://agnes-compatible-gateway.zo.deno.net`                                     |
| Platform observation    | Deno Deploy `deployd`, HTTP/2, with Deno cache/trace headers                       |
| Deployment scopes       | `health`, `chat-sse`, `image-upload`, `video`                                      |
| Credential              | Disposable caller-owned Agnes key, loaded only into the opt-in local probe process |
| Generation retry policy | None                                                                               |
| Final source/tag        | `cdee3417cd344cfc3b099b0d3245242e12e728e1` / `v0.1.0`                              |
| Final GHCR index        | `sha256:008a93110cd362beffa760859e7ed25fbe85a0be2ead76fc05a001871fbbc64f`          |
| GitHub release          | `https://github.com/4x25/agnes-compatible-gateway/releases/tag/v0.1.0`             |

## Redacted results

| Probe                    | Status | Result                                                                                                                       | Request ID  |
| ------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Health                   | `200`  | JSON status shape, `Cache-Control: no-store`, and Deno Deploy platform headers                                               | Not emitted |
| CORS preflight           | `204`  | Wildcard origin, expected methods/headers, exposed gateway headers, and request ID                                           | `aac3…bdb3` |
| External Chromium        | Passed | Both languages, six responsive widths, keyboard/focus, reduced motion, and credential non-persistence                        | Not emitted |
| Chat SSE                 | `200`  | `text/event-stream`, OpenAI chunk field types, at least one JSON event, and terminal `[DONE]`                                | `3496…4ddb` |
| Multipart image edit     | `200`  | Synthetic PNG upload passed through the public edit route and returned a Base64 result field                                 | `a318…4c0f` |
| Video creation           | `200`  | Public/video/task identity fields and asynchronous metadata types                                                            | `e077…83ea` |
| Video terminal retrieval | `200`  | Reached a successful terminal status through the public stateless video-ID route and returned a URL field type               | `0836…d4e6` |
| Video content Range      | `206`  | Public content route returned byte zero with `Content-Range`; media bytes were read only for the bounded assertion           | `2382…d42d` |
| GHCR final workflow      | Passed | Published amd64/arm64, SPDX SBOM, SLSA provenance, OCI labels, non-root runtime, and remote-digest hardened container health | Public run  |
| GitHub release           | Passed | Public `v0.1.0` release exists and is neither a draft nor a prerelease                                                       | Public URL  |

## Browser and API coverage

The external Chromium run intentionally did not submit playground generation
requests because a remotely managed gateway cannot be redirected to the local
fake Agnes server. Its UI, accessibility, responsiveness, and credential-memory
checks all passed against the real deployment. The separately gated deployment
probe then exercised every platform-sensitive public workflow directly through
that same origin with a disposable caller key.

Image editing completed across the Deno Deploy request lifecycle, confirming
that the platform sustained the multipart upload and long upstream response.
Video creation, repeated stateless retrieval, terminal media resolution, and the
public byte-range proxy also completed. Generation requests were each made once;
only idempotent video reads were repeated.

## Release impact

The final
[release workflow](https://github.com/4x25/agnes-compatible-gateway/actions/runs/29671245362)
published an anonymously pullable OCI index with `linux/amd64` and
`linux/arm64`. Each architecture has its own SPDX and SLSA attestation manifest,
uses the unprivileged `deno` user, and embeds accepted source `cdee341` as both
the OCI revision and `DENO_DEPLOYMENT_ID`. The workflow inspected the remote
index and successfully started the published digest with a read-only filesystem
and all capabilities dropped.

Independent anonymous Registry inspection confirmed that tags `0.1.0`, `0.1`,
`0`, `latest`, and `sha-cdee341` all resolve to the same final index digest. The
public GitHub Release is published and is neither a draft nor a prerelease.
Together with the real Deno Deploy lifecycle, these results satisfy every M5
deployment, container, documentation, and public-release gate. M5 is complete.
