# Agnes contract evidence — 2026-07-18

[简体中文](2026-07-18-m2.zh-CN.md)

Status: **partial acceptance; M2 remains in progress**.

This record contains only bounded status and structure observations. It omits
credentials, prompts, response values, media URLs, Base64 data, and upstream
error bodies.

## Run metadata

| Item          | Value                                                           |
| ------------- | --------------------------------------------------------------- |
| Gateway SHA   | `5012b62033c5269932e087ff1ceab010cc255fba`                      |
| Deno          | `2.9.3`                                                         |
| Upstream host | `apihub.agnes-ai.com`                                           |
| Chat scopes   | `chat`, `chat-sse`, `chat-tools`, `errors`                      |
| Image scopes  | `image`, `image-base64`, `image-edit`                           |
| Retry policy  | No generation request was retried                               |
| Credential    | Disposable caller key; loaded only into the opt-in test process |

## Redacted results

| Probe                  | Status  | Result                                                                                                            | Request ID     |
| ---------------------- | ------- | ----------------------------------------------------------------------------------------------------------------- | -------------- |
| Chat JSON              | `200`   | Passed; OpenAI-style ID, model, choices, usage, and metadata field types were present                             | `a1d0…-IAD`    |
| Chat SSE               | `200`   | Passed; JSON data events used the expected chunk fields and the stream ended with `[DONE]`                        | `a1d0…-IAD`    |
| Forced tool call       | `200`   | Passed; the assistant message contained a standard function tool call                                             | `a1d0…-IAD`    |
| Tool-result round trip | `200`   | Passed; Agnes accepted the standard assistant tool call plus `tool` result sequence                               | `a1d0…-IAD`    |
| Invalid request        | `400`   | Passed; the body shape was `{ error: { code, message, type } }`                                                   | `a1d0…-IAD`    |
| Invalid credential     | `401`   | Passed with the probe's fixed fake token; the body shape was `{ error: { code, message, type } }`                 | `a1d0…-IAD`    |
| Image URL output       | `503`   | Blocked by upstream availability after waiting for the response; the response body was intentionally not retained | Not emitted    |
| Image Base64 output    | Not run | Skipped because probes are sequential and the preceding billable generation failed                                | Not applicable |
| Data-URI image edit    | Not run | Skipped because probes are sequential and the preceding billable generation failed                                | Not applicable |

## Milestone impact

The successful Chat and error probes confirm the upstream envelopes exercised by
the gateway's existing Chat implementation. The tool-result observation does not
expand the gateway's public message-role subset; that remains a documented
compatibility gap.

The `503` is an upstream availability result, not evidence that the Image
request or response contract is incompatible. In accordance with the project's
no-retry policy, the request was not repeated and the later Image probes were
not started. M2 must remain in progress until URL output, Base64 output, and
Data-URI image editing all complete successfully in a later controlled run.
