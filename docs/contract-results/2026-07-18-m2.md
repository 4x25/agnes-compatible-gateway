# Agnes contract evidence — 2026-07-18

[简体中文](2026-07-18-m2.zh-CN.md)

Status: **accepted; M2 complete**.

This record contains only bounded status and structure observations. It omits
credentials, prompts, response values, media URLs, Base64 data, and upstream
error bodies.

## Run metadata

| Item          | Value                                                                                                                                                                                                                                                                  |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Gateway SHAs  | `5012b62033c5269932e087ff1ceab010cc255fba`, `9f462d56edc839f5d673c433766c2e127ad03a26`, `4bc0ad1e4f4e7612021b32befb5b1d6d9459c127`, `5a8bd1269686b707d7e352dab6991102dd41fd82`, `daecebb3c5788fcba59d3968517529e4c0ec002a`, `7ec1d462773f161fb194fbc955368dde9a1cba9b` |
| Deno          | `2.9.3`                                                                                                                                                                                                                                                                |
| Upstream host | `apihub.agnes-ai.com`                                                                                                                                                                                                                                                  |
| Chat scopes   | `chat`, `chat-sse`, `chat-tools`, `errors`                                                                                                                                                                                                                             |
| Image scopes  | `image`, `image-base64`, `image-edit`                                                                                                                                                                                                                                  |
| Retry policy  | No in-process retry; follow-up runs were started explicitly                                                                                                                                                                                                            |
| Credential    | Disposable caller key; loaded only into the opt-in test process                                                                                                                                                                                                        |

## Redacted results

| Probe                      | Status                           | Result                                                                                               | Request ID                              |
| -------------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------- |
| Chat JSON                  | `200`                            | Passed; OpenAI-style ID, model, choices, usage, and metadata field types were present                | `a1d0…-IAD`                             |
| Chat SSE                   | `200`                            | Passed; JSON data events used the expected chunk fields and the stream ended with `[DONE]`           | `a1d0…-IAD`                             |
| Forced tool call           | `200`                            | Passed; the assistant message contained a standard function tool call                                | `a1d0…-IAD`                             |
| Tool-result round trip     | `200`                            | Passed; Agnes accepted the standard assistant tool call plus `tool` result sequence                  | `a1d0…-IAD`                             |
| Invalid request            | `400`                            | Passed; the body shape was `{ error: { code, message, type } }`                                      | `a1d0…-IAD`                             |
| Invalid credential         | `401`                            | Passed with the probe's fixed fake token; the body shape was `{ error: { code, message, type } }`    | `a1d0…-IAD`                             |
| Image URL output           | `503` → `200`                    | The first run was unavailable; the explicit follow-up passed with `url: string` and `b64_json: null` | Not emitted → `a1d0…-IAD`               |
| Base64 via `return_base64` | `200` → `503` → `504` ×2 → `200` | Both successful responses returned `url: string` and `b64_json: null`; other runs were unavailable   | Not emitted → `a1d2…-IAD`               |
| Base64 via `extra_body`    | `200` → `503` ×2 → `200`         | The targeted request and final committed scope passed with `b64_json: string` and `url: null`        | `a1d2…-IAD` → not emitted → `a1d3…-IAD` |
| Data-URI image edit        | `200`                            | Passed; the result contained string URL, Base64, revised-prompt, and task-ID fields                  | `a1d1…-IAD`                             |

## Milestone impact

The successful Chat and error probes confirm the upstream envelopes exercised by
the gateway's existing Chat implementation. The tool-result observation does not
expand the gateway's public message-role subset; that remains a documented
compatibility gap.

An explicit follow-up confirms the Image URL-output contract. The Agnes
documentation checked on this date says that text-to-image Base64 uses
`return_base64: true`, but two successful requests returned URLs with
`data[0].b64_json: null`. A targeted request using
`extra_body.response_format: "b64_json"` returned the documented Base64
envelope, so gateway commit `daecebb` maps the standard OpenAI field to that
verified control. It does not add an automatic fallback or a second billable
request.

The separately requested Data-URI image-edit probe passed and confirms Agnes
accepts `extra_body.image` with the same Base64 control. After two transient
`503` responses, the formal `image-base64` scope passed on committed gateway
source with `b64_json: string` and `url: null`.

All M2 live scopes have now passed: Chat JSON/SSE/tools, safe error envelopes,
Image URL/Base64 output, and Data-URI image editing. Together with the ordinary
gateway and SDK suite, this completes M2. The probe records bounded response
shapes before assertions, so future drift can be diagnosed without retaining
response values.
