# Agnes 视频契约证据 — 2026-07-18

[English](2026-07-18-m3.md)

状态：**验收通过；M3 已完成**。

本文只记录有长度限制的状态和结构观察结果，不包含凭据、提示词、ID、响应值、媒体
URL、Data URI 或媒体字节。

## 运行信息

| 项目            | 值                                                     |
| --------------- | ------------------------------------------------------ |
| 网关 SHA        | `21b8e709dc57681858fc70a643e6a8e6d62a18ce`             |
| Deno            | `2.9.3`                                                |
| 配置的 API 主机 | `load.980425.xyz`（调用方自管的 Agnes HTTPS 反向代理） |
| Scope           | `video`，启用完成态轮询                                |
| 输入            | 仓库自带的合成 PNG Data URI，不含用户数据              |
| 重试策略        | 创建请求绝不重试；只重复幂等查询                       |
| 凭据            | 一次性调用方 Key；只加载到显式启用的实时测试进程       |

## 脱敏结果

| 探测项               | 状态  | 结果                                                                                            | 请求 ID     |
| -------------------- | ----- | ----------------------------------------------------------------------------------------------- | ----------- |
| 首次创建观察         | `200` | 返回字符串类型的 `id`、`video_id`、`task_id`、`status`、`seconds` 与 `size`                     | 已脱敏      |
| 旧版 task-ID 查询    | `400` | 拒绝刚创建的任务，否定了网关最初采用旧版路由的假设                                              | 已脱敏      |
| 正式视频创建         | `200` | 返回三个预期身份字段及 OpenAI 风格异步元数据类型                                                | `a1d3…-IAD` |
| 推荐 video-ID 轮询   | `200` | 通过 `/agnesapi?video_id=…` 到达成功终态，并返回进度、耗时、尺寸与 URL 字段类型                 | `a1d3…-IAD` |
| 成品内容字节范围下载 | `206` | `Range: bytes=0-0` 返回 `Content-Range` 与 `Content-Type`，且未向媒体源转发 Agnes Authorization | 未提供      |

## 对里程碑的影响

首次实时观察确认创建成功，但文档中的旧版 `/v1/videos/{task_id}` 查询拒绝新任务；
创建响应同时提供了独立的 `video_id`。正式验收确认该身份可通过推荐的无状态
`/agnesapi` 接口直接查询。

因此提交 `21b8e70` 将 Agnes `video_id` 暴露为 OpenAI 风格公开 `id`，保留
`task_id` 扩展，并将视频查询与内容解析切换到推荐的 video-ID 路由。推荐接口返回
`400`/`404` 时只进行一次旧版只读回退，使旧版网关曾返回的 ID 仍可使用。整个实现
仍完全无状态，不增加数据库、缓存、队列或生成请求重试。

本次实时运行前，提交后的实现已通过格式、lint、类型检查、全部 42 项
网关/SDK/OpenAPI/Fresh 测试、生产构建及完整 Chromium smoke。真实创建、终态轮询、
成品媒体 URL 解析和 Range 下载现已全部通过，因此 M3 正式完成。
